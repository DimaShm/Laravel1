### Getting started

Build and start the services:

```shell
make build up
```

Install the application:

```shell
make composer-install db-create
```

Init git hooks:

```shell
make init-githooks
```

